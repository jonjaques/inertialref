# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# InertialRef
.tsbuildinfo
*.tsbuildinfo
coverage
.vitest

# Raw catalogue downloads. 34 MB of HYG to produce a 460 KB asset, and the asset
# is what the game needs — `pnpm catalog:fetch` re-downloads them, and
# `data/catalog/manifest.json` records the digest of exactly what was used.
.data/

# The reference edit's audio track, for the cutscene's optional sync. A local
# copy of copyrighted music: it must never enter the repository. The overlay
# discovers it at this path when present and plays without it when not.
apps/game/public/tng-intro.*
