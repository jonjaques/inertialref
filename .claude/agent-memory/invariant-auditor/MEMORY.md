# Memory index

- [Audit patterns](audit-patterns.md) — where invariant violations cluster in InertialRef: the sketch cache key that drifts from what `derive` reads, order-dependent fixtures, memo keys that omit the eye, tests that cannot fail, `Observatory.focus` side effects, AGENTS.md mirror drift and the newer failure of a rule landing in the wrong path scope, fix-pass residue, surviving twin constants, docs flowcharts vs new predicates, drifting count words and missing ADR index rows, a figure restated in five places and corrected in one — including the inverse, where the _constant_ is corrected and five prose sites still argue the retired one, false comment premises, why `<Routes location>` makes raw-pathname findings inside a mode false, why you should read `node_modules/three` before filing a TSL semantics finding, precedence getters resolved for the wrong consumer, `JSON.stringify(Infinity)` in a persisted preference, host ports that write values React owns, device-vs-CSS pixels in `pixelAngle`, ADR "provable by grep" claims, coverage guarantees the lattice arithmetic does not provide, octave counts off by one, C0 steps at a profile's inner branch boundary and the exact-rotation scan that finds them, normalizers that omit their own damping factor, the dossier stating a generated number as a measured fact, a cache layer added in front of another disarming the test behind it, a cap sized from one selection when the keep set is the union of two, a regression bound taken from the theoretical floor, the `git archive` (not `git worktree`) discipline for measuring a moving tree, prose worked-examples the generator does not produce, the docstring figure that matches the test fixture rather than the code, memo keys checked against the callee's parameter type, the `git archive` two-revision diff that settles an "identical answer, fewer allocations" refactor, the one grep that proves an address is immutable enough for a `WeakMap`, the advisory essay that turns a path-scoped mirror into a 108-line read, the `.mjs` query-key twin nothing holds to `QUERY`, why the concurrent docs pass sweeps prose but never `.claude/rules/` or a Mermaid diagram, two rig counters with different populations combined into a ratio that does not exist, one measurement quoted as three numbers because vitest prints both a `Duration` and a wall clock, `CONTEXT.md` § "Known gaps" as the present-tense section nothing sweeps, a shipped plan's premise block asserting what two files still say, the `git archive` + `vitest.mjs` recipe that proves a GPU regression test is armed, a constant moving while its own file keeps the retired word-form 900 lines down, a docstring citing a budget doc that says the opposite, the per-package `node_modules` symlink a cross-revision import needs, a "every Sol body" premise one loop from being falsified (82 of 128), and why `warmAtMount` from a renderer-ready callback is never a census unit, the font-stack resolver that proved `☉` and `⊕` still fall to the platform, the new `:coverage` script that does not complete, a gate header quoting the low one of three `pnpm test` figures, `describe.skip` as a cost decision that also empties CI, `@ts-expect-error` suppressing the dead-code signal over a commented-out log, labels walked back from title case with the rule left standing in four places, a branch that violates its own new invariant in the one file the sweep missed, an identifier rename that sweeps AGENTS.md and its mirror and leaves the ADR that quotes it, a derived per-entity cache a new movement path bypasses, the `runTicks`-vs-`step()`-loop proof of jump equivalence and the `commitTicks` monkeypatch that counts jumps, why a sound bound on fixed boundaries converges whatever its schedule, the ground band measured at 0.46× its own premise, and a modulo test that reduces the way the function does
- [Timeline audit](timeline-audit.md) — the instrumentation-seam shapes: a mirror that drifted _stricter_ than AGENTS.md, `paths:` naming the seam and not the call sites, a bounded-label rule obeyed where it was argued and broken in an interpolated float key, `?? 25` under a docstring saying "not defaulted here", and the mutation test that proves a comment-stripping grep guard
