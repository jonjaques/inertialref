# Memory index

- [Audit patterns](audit-patterns.md) — where invariant violations cluster in InertialRef: order-dependent fixtures, tests that cannot fail, `Observatory.focus` side effects, AGENTS.md mirror drift, fix-pass residue, surviving twin constants, docs flowcharts vs new predicates, allow-list gates broken by the follow-up docs commit, drifting count words, false comment premises, and why `<Routes location>` makes raw-pathname findings inside a mode false, precedence getters resolved for the wrong consumer, `JSON.stringify(Infinity)` in a persisted preference, a branch's own re-measurement falsifying its module headers, host ports that write values React owns, device-vs-CSS pixels in `pixelAngle`, and ADR "provable by grep" claims the tree already violates
