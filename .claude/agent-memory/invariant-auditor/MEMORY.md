# Memory index

- [Audit patterns](audit-patterns.md) — where invariant violations cluster in InertialRef: order-dependent "found not authored" fixtures, tests that cannot fail, `Observatory.focus` side effects, amended-AGENTS.md mirror drift, `text-slate-500`, and what `pnpm graph` misses
