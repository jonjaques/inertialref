# Memory index

- [Audit patterns](audit-patterns.md) — where invariant violations cluster in InertialRef: order-dependent fixtures and additive memo keys, tests that cannot fail, `Observatory.focus` side effects, AGENTS.md mirror drift, fix-pass residue, surviving twin constants, docs flowcharts vs new predicates, drifting count words, a measured figure restated in five places and corrected in one, false comment premises, why `<Routes location>` makes raw-pathname findings inside a mode false, precedence getters resolved for the wrong consumer, `JSON.stringify(Infinity)` in a persisted preference, host ports that write values React owns, device-vs-CSS pixels in `pixelAngle`, ADR "provable by grep" claims the tree already violates, coverage guarantees the lattice arithmetic does not provide, C0 steps at a profile's inner branch boundary, normalizers that omit their own damping factor, the dossier stating a generated number as a measured fact, a cache layer added in front of another disarming the test behind it, retired figures returning inside newly authored prose, the rank-seam class now at three sites with no AGENTS bullet, a cap sized from one selection when the keep set is the union of two, a regression bound taken from the theoretical floor rather than the measurement, and the two-detached-worktree discipline for measuring a moving tree
